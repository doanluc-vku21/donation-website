"use client";
import { useEffect,useState } from "react";
import { Heart } from "lucide-react";
export function MobileDonateBar(){const [visible,setVisible]=useState(false);useEffect(()=>{const panel=document.getElementById("donation-panel");if(!panel||typeof IntersectionObserver==="undefined")return;const observer=new IntersectionObserver(([entry])=>setVisible(!entry.isIntersecting),{threshold:.05});observer.observe(panel);return()=>observer.disconnect()},[]);if(!visible)return null;return <div className="fixed inset-x-0 bottom-0 z-40 border-t border-[var(--border)] bg-white/92 p-3 backdrop-blur-lg lg:hidden"><button type="button" onClick={()=>document.getElementById("donation-panel")?.scrollIntoView({behavior:"smooth",block:"start"})} className="mx-auto flex min-h-12 w-full max-w-lg items-center justify-center gap-2 rounded-2xl bg-[var(--accent)] px-5 font-semibold text-white shadow-lg"><Heart aria-hidden="true" className="size-4 fill-current"/> Donate now</button></div>}
